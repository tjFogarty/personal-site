{% extends 'layouts/default' %}

{% block content %}

  <div class="l-home-page">
    <div>
      <h1>TJ Fogarty</h1>
    </div>

    <div>
      {% cache %}
      {% include 'posts/_latest' %}
      {% endcache %}
    </div>
  </div>

{% endblock %}