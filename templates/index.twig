{% extends 'layouts/default' %}

{% block content %}
  <div class="c-hero">
    <div class="c-hero__content">
      <div class="l-container">
        <h1 class="c-hero__title">TJ Fogarty</h1>
        <p class="c-hero__subtitle">Web Developer</p>
      </div>
    </div>
    <img class="c-hero__image" src="/images/tramore.jpg" alt="Tramore Beach, County Waterford" />
  </div>

  <div class="l-container">
    {# {% cache %} #}
    {% include 'posts/_latest' %}
    {# {% endcache %} #}
  </div>

{% endblock %}