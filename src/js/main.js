import { ready } from './utils'

ready(() => {
  // let pre = document.querySelector('pre')
  
  // if (pre) {
  //   const Prism = System.import('prismjs').then(Prism => {
  //     Prism.highlightAll()
  //     console.log(Prism)
  //   })
  // }
})